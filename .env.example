# Adzuna API credentials — get a free app_id + app_key at https://developer.adzuna.com/
# Copy this file to `.env` and fill in the values. `.env` is git-ignored; never commit it.
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
