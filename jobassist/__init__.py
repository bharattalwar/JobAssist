"""jobAssist — find US jobs at employers likely to transfer an H1B, ranked to a resume.

A small, single-purpose CLI. Run it with `python -m jobassist --resume <path-to-resume.pdf>`.
"""

__version__ = "0.1.0"
